# # Include go and cargo in path
# export GOPATH=$HOME/.go
# # export PATH="$PATH:$(yarn global bin)"

# export PATH=$PATH:~/bin:${GOPATH//://bin:}/bin:${HOME}/.cargo/bin:${HOME}/.local/bin:$HOME/.poetry/bin:${KREW_ROOT:-$HOME/.krew}/bin
# export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-agent.socket"

# export EDITOR=nvim
# export VISUAL=nvim
# export ALTERNATIVE_EDITOR=vi
# alias :e=nvim
# alias :E=sudoedit

# # Use UTF-8
# export LANG=en_US.UTF-8

# if [[ -r ~/.config/zsh/local.zsh ]]; then
#         source ~/.config/zsh/local.zsh
# fi
# source "$HOME/.cargo/env"
# export LBC_VERSION="v2.0.0"

# # export LD_LIBRARY_PATH="/usr/lib64"
# # export LIBRARY_PATH="/usr/lib64"
# # export PKG_CONFIG_PATH="/usr/lib64/pkgconfig"

# . "$HOME/.nix-profile/etc/profile.d/nix.sh"
# . ~/.nix-profile/etc/profile.d/hm-session-vars.sh

